version: 2

conda:
  environment: docs/environment.yml

build:
  image: testing
  
formats:
  - epub
  - pdf

python:
  version: 3.9
  install:
    - method: pip
      path: .
      extra_requirements: 
        - docs
  system_packages: true

sphinx:
  configuration: docs/conf.py

